"use client";

import React, { useState, useRef } from "react";
import { Box } from "@mui/material";

export default function Dashboard() {
  return (
    <Box
      sx={{
        width: "100vw",
        height: "100%",
        color: "black",
        backgroundColor: "white",
      }}
    >
      <Box p="20px">FIOH Image Submissions</Box>
    </Box>
  );
}
